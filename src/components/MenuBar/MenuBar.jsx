import React, { Component } from 'react';
import { Layout } from 'antd';
const { Sider } = Layout;

export default class extends Component{
    render(){
        return(
            <div>
                
            </div>
        )
    }
}